@model FileInfo[]
@{
    Layout = "_Layout";
}
<h3>File List</h3>
<div class="row">
    <a asp-action="Create" asp-controller="File">Create</a>
    <table class="table table-hover table-bordered">
        <thead>
            <tr>
                <th>File Name</th>
                <th>CreationTime</th>
                <th>Path</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.CreationTime</td>
                    <td>@item.FullName</td>
                    <td><a asp-action="Update" asp-controller="File" asp-route-fileName="@item.FullName">Update</a></td>
                    <td><a asp-action="Delete" asp-controller="File" asp-route-fileName="@item.FullName">Delete</a></td>
                </tr>
            }
        </tbody>
    </table>
</div>